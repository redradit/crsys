# Cryptographic primitives (X25519, Ed25519, HKDF, ChaCha20-Poly1305, AES-GCM, scrypt)
cryptography>=42.0

# Graphical interface (not needed for the library or the CLI)
customtkinter>=5.2

# Recommended: enables Argon2id instead of scrypt for private key protection.
# Optional in the sense that scrypt is used when it is absent, but a key file
# written with Argon2id cannot be opened without it.
argon2-cffi>=23.1
